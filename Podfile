# Uncomment the next line to define a global platform for your project

platform :ios, '11.0'

use_frameworks!

# ignore all warnings from all pods
inhibit_all_warnings!

def shared_pods
  # Pods for Skeleton
  pod 'TwoWayBondage', '~> 2.0.1'
  pod 'Parchment', '~> 1.7.0'
end

target 'RRMultipleDataSourceProposal' do
  shared_pods
end
